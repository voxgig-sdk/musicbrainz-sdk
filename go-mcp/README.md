# musicbrainz-mcp

MCP server exposing the Musicbrainz SDK as tools, built on the
[official Go MCP SDK](https://github.com/modelcontextprotocol/go-sdk)
and the sibling Go SDK at `../go`.

## Tools

| Tool | Args | Returns |
|------|------|---------|
| `musicbrainz_list` | `entity`, optional `query` map | First page of records as JSON |
| `musicbrainz_load` | `entity`, `query` (e.g. `{id:N}`) | Single record as JSON |

JSON schemas for both tools are emitted by the SDK from the `Args`
struct's `json` / `jsonschema` tags — no schema is hand-written.

## Entities

area | artist | collection | event | genre | instrument | label | place | rating | recording | recording_list | release | release_group | release_list | series | tag | url | work | work_list

## Build

```sh
go build -o musicbrainz-mcp ./...
```

## Run

```sh
# stdio transport — for spawnable agent installs (default).
./musicbrainz-mcp

# streamable HTTP transport — share one running server between agents.
./musicbrainz-mcp -transport http -addr :8080
```

## Install for Claude Code

```sh
claude mcp add --scope user musicbrainz \
  -- /absolute/path/to/musicbrainz-mcp -transport stdio
```

After install, restart Claude Code; the `musicbrainz_list` and
`musicbrainz_load` tools become available in new sessions.

## Smoke test via HTTP

```sh
./musicbrainz-mcp -transport http -addr :18080 &

# initialize, grab the session id
curl -sN -X POST http://localhost:18080 \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json, text/event-stream' \
  -D headers \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"smoke","version":"0"}}}'

SESSION=$(awk '/Mcp-Session-Id/ {print $2}' headers | tr -d '\r')

curl -sN -X POST http://localhost:18080 \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json, text/event-stream' \
  -H "Mcp-Session-Id: $SESSION" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"musicbrainz_load","arguments":{"entity":"area","query":{"id":1}}}}'
```

## Generated by

sdkgen `go-mcp` target. See the target source under
`.sdk/src/cmp/go-mcp/` in this repo, or upstream at
`github.com/voxgig/sdkgen/project/.sdk/src/cmp/go-mcp/`.
